<div>
  <h2>Your Reads</h2>
  {{#each model as |book|}}
  {{!-- {{#if model.books}} --}}
    <h3>
      {{#link-to 'book' book}}
      {{book.title}}
      {{!-- <button {{action 'deleteBook' book}}>Delete Book</button>
      {{input value=book.title}}
      <button {{action 'editBook' book}}>Edit Book</button>--}}
      {{/link-to}}
    </h3>
  {{/each}}
</div>

{{#link-to 'books.new'}}<button>Add Book</button>{{/link-to}}
{{!-- <h5>You don't have any books on your list yet</h5>
{{#link-to 'books.new'}}<button>Add Book</button>{{/link-to}} --}}
{{outlet}}
<div class="spacer"></div>
<div class="spacer"></div>
<div class="spacer"></div>
